services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sirena
      POSTGRES_USER: sirena
      POSTGRES_PASSWORD: sirena123
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
